events {}

http {
    server {
        listen 80;

        location / {
            proxy_pass http://frontend:80/;
        }

        location /api/ {
            proxy_pass http://backend:5001;
        }

        location = /init {
            proxy_pass http://backend:5001/init;
        }
    }
}

